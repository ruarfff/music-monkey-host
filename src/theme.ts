import { createMuiTheme } from 'material-ui/styles'

const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#00838f'     
    },
    secondary: {      
      main: '#90a4ae'    
    }
  }
})

export default theme
